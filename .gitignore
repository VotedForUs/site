# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
debug-storybook.log
*.log


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# API cache — raw responses, not for version control
.cache/

# benchmark results — local only
benchmark-results/
dist-cache/

# incremental build artifacts — generated, not for version control
.current-digests.json
.loader-digests.json
src/data/.build-manifest.json

# PR body for create-pull-request (generated in CI, not committed)
.github/pr-body.md